git init -- bare -> do this in local repository

(create a new directory for your repository)

git clone (the path of your repo) -> in cloning your repo

git status to show the status of your git.

git add (filename) -> to add to the original repository

git commit -m "added readme file" -> to commiting file

git push origin master -> to push

git log -> to see the logs 

touch a.php -> gagawa ng files

git added . -> period means all

git show (the id) -> to show the content 

modified -> mean napalitan 

git dif -> to show the diff.

add, commit, push

to combine the add and commit you can use this.
(git commit -am "messege" and the )

git reset -> if you reset even the logs will reset

git revert (hashcat_id) -> means undo in the git repository

git checkout -> para mabalik mo lahat or maremove mo yung nasa repository mo

git remote add origin [URL]

git init

git init --bare